## Variant Filtering
Hard-filtering of SNPs from joint-genotyped VCFs using GATK best-practices annotations followed by stringent VCFtools filtering for depth (4–100), quality (GQ ≥ 10, QUAL ≥ 30), genotyping completeness (≥50%), and MAF (≥0.0001).
